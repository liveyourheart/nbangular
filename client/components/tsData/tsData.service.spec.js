'use strict';

describe('Service: tsData', function () {

  // load the service's module
  beforeEach(module('nbaPlaygroundApp'));

  // instantiate service
  var tsData;
  // beforeEach(inject(function (_tsData_) {
  //   tsData = _tsData_;
  // }));
  //
  // it('should do something', function () {
  //   expect(!!tsData).toBe(true);
  // });

});
